---
- name: "Molecule test"
  ansible.builtin.command:
    cmd: molecule test
    chdir: "{{ playbook_dir | regex_search('.+\/ansible_collections\/.+?\/.+?(?=\/)') }}/roles/node_exporter"
  changed_when: false
